var chalk = require('chalk');var inquirer = require('inquirer');var nameInit = [{    name: 'appName',    type: 'input',    message: '输入项目名' + chalk.gray('(例: 后台系统)： '),    validate(value) {        if (value.length) {            return true;        } else {            return '请输入您项目的名称';        }    }}];var author = [{    name: 'author',    type: 'input',    message: '作者' + chalk.gray(' <宋宝文>：'),    validate(value) {        if (value.length) {            return true;        } else {            return '请输入创建人的名称';        }    }}];module.exports = {    nameInit: nameInit,    author: author};